diff --git a/core/build_rro_package.mk b/core/build_rro_package.mk
index ae528bddc4..f27841d9b6 100644
--- a/core/build_rro_package.mk
+++ b/core/build_rro_package.mk
@@ -29,7 +29,7 @@ endif
 ifeq ($(LOCAL_RRO_THEME),)
   LOCAL_MODULE_PATH := $(partition)/overlay
 else
-  LOCAL_MODULE_PATH := $(partition)/overlay/$(LOCAL_RRO_THEME)
+  LOCAL_MODULE_PATH := $(partition)/overlay/$(subst Lineage,Strix,$(LOCAL_RRO_THEME))
 endif
 
 # Do not remove resources without default values nor dedupe resource
diff --git a/tools/releasetools/build_image.py b/tools/releasetools/build_image.py
index 4d59ceee40..8a5382f27f 100755
--- a/tools/releasetools/build_image.py
+++ b/tools/releasetools/build_image.py
@@ -39,6 +39,7 @@ import tempfile
 import common
 import verity_utils
 
+from xml.etree import ElementTree as ET
 
 logger = logging.getLogger(__name__)
 
@@ -506,6 +507,23 @@ def SetUUIDIfNotExist(image_props):
   hash_seed = "hash_seed-" + uuid_seed
   image_props["hash_seed"] = str(uuid.uuid5(uuid.NAMESPACE_URL, hash_seed))
 
+TARGET_DIRS = ["etc"]
+
+def strip_xml_comments(root):
+    for d in TARGET_DIRS:
+        p = os.path.join(root, d)
+        if not os.path.isdir(p):
+            continue
+        for dirpath, _, filenames in os.walk(p):
+            for name in filenames:
+                if name.endswith(".xml"):
+                    file = os.path.join(dirpath, name)
+                    try:
+                        parser = ET.XMLParser(target=ET.TreeBuilder(insert_comments=False))
+                        tree = ET.parse(file, parser)
+                        tree.write(file, encoding="utf-8")
+                    except:
+                        pass
 
 def BuildImage(in_dir, prop_dict, out_file, target_out=None, otatools_dir=None):
   """Builds an image for the files under in_dir and writes it to out_file.
@@ -523,6 +541,8 @@ def BuildImage(in_dir, prop_dict, out_file, target_out=None, otatools_dir=None):
   Raises:
     BuildImageError: On build image failures.
   """
+  strip_xml_comments(in_dir)
+
   in_dir, fs_config = SetUpInDirAndFsConfig(in_dir, prop_dict)
   SetUUIDIfNotExist(prop_dict)
 
